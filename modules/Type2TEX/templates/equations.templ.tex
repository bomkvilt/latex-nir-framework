${'%'} DON'T EDIT - this file was generated automaticaly by a 'framefork'.'Type2TeX' tool
${'%'} This file represents an original MathType equation


${'%%% prints equations'}
${'%   #1 - label      '}
\DeclareDocumentCommand{\printEquations}{ o }
{
    <% eqs = vars['equations'] %>

    % if vars['mode'] == 'lines':
    % for eq in eqs:
    \begin{equation}
        % if   len(eqs) > 1 and eq['label'] != '':
        \IfValueT{#1} { \label{#1--${eq['label']}} }
        % elif len(eqs) == 1:
        \IfValueT{#1} { \label{#1} }
        % endif
        ${eq['text']}
    \end{equation}
    % endfor

    % elif vars['mode'] == 'flat':
    \begin{equation} 
        \IfValueT{#1} { \label{#1} }
        \begin{array}{l}
            % for eq in eqs:
            ${eq['text']} \\\
            
            % endfor
        \end{array} 
    \end{equation}
    % endif
}


${'%%% prints variable explonation table'}
${'%   #1  - "where:"                   '}
\DeclareDocumentCommand{\printVarExplTable}{ m }
{
    <% expls = vars['varexpls'] %>
    % if expls and len(expls) > 0:
    \begin{tabu} to \textwidth {llX}
        <% expl = expls[0] %>
        #1 & ${'$'}${expl['var']}${'$'} & --- ${expl['expl']} \\\
        
        % for expl in vars['varexpls'][1:]:
        ~  & ${'$'}${expl['var']}${'$'} & --- ${expl['expl']} \\\

        % endfor

    \end{tabu}
    % endif
}
