% -----------------| 
% >>

%%% add a reference to a resource
%   #1  - prefix to be added to all passed lables
%   #2  - list of labels
\seq_new:N \l@labels@lbls_seq
\seq_new:N \l@labels@full_seq
\cs_new_protected:Npn \secs_addReference:Nn #1 #2 {
    \seq_clear:N \l@labels@full_seq
    \seq_set_split:Nnn \l@labels@lbls_seq {,}{#2}
    \seq_map_inline:Nn \l@labels@lbls_seq 
    {
        \seq_push:Nn \l@labels@full_seq {#1 ##1}
    }
    \cref{\seq_use:Nnnn \l@labels@full_seq {,}{,}{,}}
}

% >>
% -----------------| 
